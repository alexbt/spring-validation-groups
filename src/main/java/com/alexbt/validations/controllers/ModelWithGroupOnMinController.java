package com.alexbt.validations.controllers;

import com.alexbt.validations.models.ModelWithGroupOnMin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/ModelWithGroupOnMin")
public class ModelWithGroupOnMinController extends AbstractController<ModelWithGroupOnMin> {
}
