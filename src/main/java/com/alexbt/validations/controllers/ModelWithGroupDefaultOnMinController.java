package com.alexbt.validations.controllers;

import com.alexbt.validations.models.ModelWithGroupDefaultOnMin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/ModelWithGroupDefaultOnMin")
public class ModelWithGroupDefaultOnMinController extends AbstractController<ModelWithGroupDefaultOnMin> {
}
